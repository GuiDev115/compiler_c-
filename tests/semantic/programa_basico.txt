/* Programa básico em C- */
int main() {
    int x;
    int y;

    
    x = 5;
    y = 10;

    
    if (x > 0) {
        y = x + 2;
    }
    
    while (y < 20) {
        y = y + 1;
    }
    
    return 0;
}
